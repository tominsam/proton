<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:actionBarStyle">@style/AppActionBar</item>
    </style>

    <style name="AppActionBar" parent="android:Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:windowContentOverlay">@null</item><!-- remove actionbar shadow -->
        <item name="android:windowActionBarOverlay">true</item><!-- transparent actionbar -->
        <item name="android:background">@color/dark_background</item>

    </style>

</resources>
